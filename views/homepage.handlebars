  
  <div class="col-md-5">
    <h1>
        Group Projects 
    </h1>
      {{#each projects as |project| }}
    <h2>
      <a href="{{project.link}}" target="_blank">{{project.name}}</a>
    </h2>
  </div>
  <div class="col-md-7">
    <p>
      {{project.description}}
    </p>
  </div>
</div>
{{/each}}