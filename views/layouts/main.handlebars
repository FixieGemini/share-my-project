<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="stylesheet" href="/css/style.css">
    <title>Share My Project!</title>
</head>
<body class="bg-dark">
  <div class="">
    <header class="p-2 navbar navbar-secondary bg-secondary justify-content-center flex-column align-items-center">
      {{#if logged_in}}
       <a class="text-center" href="/homepage"><h1 class="home">Share My Project!</h1></a>
      {{else}}
      <h1 class="text-dark text-center">Share My Project!</h1>
      {{/if}}
      <nav>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a class="btn btn-dark" href="/profile">profile</a> |
        <button class="no-button btn btn-dark" id="logout">logout</button>
        {{/if}}
      </nav>
    </header>
    <main class="d-flex justify-content-center">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>
      
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>
</html>